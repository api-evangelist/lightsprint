generated: '2026-07-19'
method: searched
source: https://github.com/SprintsAI/lightsprint-claude-code-plugin/tree/main/skills
notes: >-
  Lightsprint publishes first-party Agent Skills as part of its Claude Code plugin
  (npm `lightsprint`). Each SKILL.md is saved verbatim from the SprintsAI GitHub org;
  none of these were generated by the enrichment pipeline. The skills drive the
  `lightsprint` CLI, which in turn calls the Lightsprint REST API under OAuth 2.0.
distribution:
  plugin: lightsprint
  install: npx lightsprint
  marketplace: https://github.com/SprintsAI/lightsprint-claude-code-plugin
  invocation: /lightsprint:<skill>
skills:
- file: lightsprint-tasks.md
  name: tasks
  command: /lightsprint:tasks
  description: List tasks from the Lightsprint workspace board.
  api: Lightsprint API
  operations:
  - GET /api/repos/{repoId}/tasks
- file: lightsprint-create.md
  name: create
  command: /lightsprint:create
  description: Create a new task in the workspace default stack.
  api: Lightsprint API
  operations:
  - POST /api/repos/{repoId}/tasks
- file: lightsprint-update.md
  name: update
  command: /lightsprint:update
  description: Update an existing task's title, description, status, complexity, assignee,
    position, or dependencies.
  api: Lightsprint API
  operations:
  - PATCH /api/tasks/{taskId}
- file: lightsprint-get.md
  name: get
  command: /lightsprint:get
  description: Get full details of a task by ID.
  api: Lightsprint API
  operations:
  - GET /api/tasks/{taskId}
- file: lightsprint-claim.md
  name: claim
  command: /lightsprint:claim
  description: Claim a task — sets it to in_progress and links the Claude Code session.
  api: Lightsprint API
  operations:
  - POST /api/tasks/{taskId}/claim
- file: lightsprint-comment.md
  name: comment
  command: /lightsprint:comment
  description: Add a comment to a task.
  api: Lightsprint API
  operations:
  - POST /api/tasks/{taskId}/comments
